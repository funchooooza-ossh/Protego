apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: protego-postgres-pvc
  namespace: kube-system
spec:
  storageClassName: postgres
  volumeName: protego-postgres-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
