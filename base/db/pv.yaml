apiVersion: v1
kind: PersistentVolume
metadata:
  name: protego-postgres-pv
spec:
  storageClassName: postgres
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/data/protego/postgres"
