apiVersion: v1
kind: Service
metadata:
  name: protego-db
  namespace: kube-system
spec:
  type: ClusterIP
  selector:
    app: protego-db
  ports:
    - name: "entry"
      port: 5432
      targetPort: entry

